# pulsar/

Intentional placeholder. Topics are created by the `pulsar-init` service in
`docker-compose.yml` (partitioned application topics + one `.dlq` per topic).
Keep that list in sync with `src/BuildingBlocks/OrderFlow.Messaging/PulsarTopics.cs`.

Put broker policy / namespace setup scripts here if the standalone defaults stop being enough.
